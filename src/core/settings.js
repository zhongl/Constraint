const settings = {
    // need restart
    useStats: false,
    textureSize: 128,
    lineAmount: 65536,

    // lines
    followMouse: false,
    constraintRatio: 0.07,
    useWhiteNodes: false,
    whiteNodesRatio: 1,

    useReflectedGround: true,

    isWhite: false,
    whiteRatio: 0
};

export default settings;
